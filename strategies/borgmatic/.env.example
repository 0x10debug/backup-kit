# strategies/borgmatic/.env.example
# Copy to .env and fill in your credentials. NEVER commit the real .env.

# ── Required ─────────────────────────────────────────────────────────────────
BORG_REPO=
BORG_PASSPHRASE=

# ── Optional (defaults shown) ────────────────────────────────────────────────
# BACKUP_PATHS=/data
# RETENTION_DAILY=7
# RETENTION_WEEKLY=4
# RETENTION_MONTHLY=6
